
To run this application, you need to have Python installed on your system. You can download it from the official Python website. Once Python is installed, you can run this application using the following command:

py -m pip install statsmodels
py -m pip install matplotlib
py -m pip install pandas
py -m pip install sqlalchemy


# ArimaS
An Application for a Clothingg Store with ARIMA and Tree Forecast thingy


Admin password:
Admin Admin@1234